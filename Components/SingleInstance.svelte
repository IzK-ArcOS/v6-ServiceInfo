<p>
  The Single Instance property specifies if you can open only one instance of
  any given application at once, or multiple.<br /><br />
  If Single Instance is enabled, and a second instance is spawned, it will stop the
  spawn and focus the first instance instead.
</p>
